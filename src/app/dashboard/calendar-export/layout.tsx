import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Kalender-Export",
};

export default function CalendarExportLayout({ children }: { children: React.ReactNode }) {
  return children;
}
