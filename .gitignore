node_modules/
.next/
tsconfig.tsbuildinfo
bin/
.DS_Store
*.log
.env
.env.*
!.env.*.example

# Bun --compile temp artifacts
*.bun-build

# Lock files (project uses bun.lock)
package-lock.json

# IDE
.idea
.vscode/*
!.vscode/settings.json

# Agent workspace artifacts
.claude/
.context/
.data/
/export/

# Devin DoD-Gate runtime markers (nicht committen — pro Session)
.devin/.last-edit
.devin/.dod-gate-passed

# DB backups (lokal erzeugt, nicht ins Repo)
backups/

# Test artifacts
test/.cache/
test-results/
playwright-report/
test/fixtures/pglite-snapshot.tar
test/fixtures/pglite-snapshot.version
server/test/fixtures/longmemeval-full/

# T2.6 Test Gate artifacts (persisted via CI upload-artifact, not in repo)
server/src/eval/test-gate/artifacts/
server/src/eval/test-gate/trends/

# Transient artifacts
tmp-legal-repo/
bulk-import-cursor.json
law-corpus-split/
*.bak

# Legal corpus is DATA (26GB, ~772k files) — lives outside the app tree at
# SUBSUMIO_LAW_CORPUS_DIR (see src/lib/corpus-paths.ts). Not part of the repo.
law-corpus/

# Deployment
.vercel

# Keep env templates tracked
!.env.example
!**/.env.example

# Local build caches / private ops notes
.next-turbo/
*.tsbuildinfo
SERVER_INVENTORY.md

# Local verification builds + ad-hoc QA artefacts (never commit)
.next-verify/
.next-e2e/
.next-isolated
.next-mock/
tests/screenshots/
tests/e2e-playwright/marketing-visual.spec.ts-snapshots/

# Office add-ins, built by scripts/build-office-addins.ts during `bun run build`
/public/word-addin/
/public/outlook-addin/

# Runtime state written by src/lib/source-registry.ts
.source-registry-diff/
