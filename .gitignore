node_modules/
.next/
tsconfig.tsbuildinfo
bin/
.DS_Store
*.log
.env
.env.*
!.env.*.example

# Bun --compile temp artifacts
*.bun-build

# Lock files (project uses bun.lock)
package-lock.json

# IDE
.idea
.vscode/*
!.vscode/settings.json

# Agent workspace artifacts
.claude/
.context/
.data/
/export/

# Devin DoD-Gate runtime markers (nicht committen — pro Session)
.devin/.last-edit
.devin/.dod-gate-passed

# Test artifacts
test/.cache/
test-results/
playwright-report/
test/fixtures/pglite-snapshot.tar
test/fixtures/pglite-snapshot.version
server/test/fixtures/longmemeval-full/

# T2.6 Test Gate artifacts (persisted via CI upload-artifact, not in repo)
server/src/eval/test-gate/artifacts/
server/src/eval/test-gate/trends/

# Transient artifacts
tmp-legal-repo/
bulk-import-cursor.json
law-corpus-split/
*.bak

# Deployment
.vercel

# Keep env templates tracked
!.env.example
!**/.env.example
