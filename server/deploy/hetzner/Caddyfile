# Auto-HTTPS reverse proxy for the Subsumio engine.
# ENGINE_DOMAIN (e.g. api.subsum.io) must have a DNS A-record pointing at this
# box BEFORE first boot — Caddy provisions a Let's Encrypt certificate on demand.
{$ENGINE_DOMAIN} {
	encode gzip
	reverse_proxy engine:3131
}
